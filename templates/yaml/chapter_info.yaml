# Access ID
iname: "{{iname}}"

# Chapter info
name: "{{name}}"
study-time: 0

# Topics
topics:
  # The _root topic is always the base topic that is used. For backwards compatibility, main is autoconverted to root
  _root:
    name: $name # Optional
    accept:
      - food
      - source
    reject:
      - deficiency
    subtopics:
