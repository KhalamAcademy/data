# Access ID
iname: "{{iname}}"

# Chapter info
name: "{{name}}"
study-time: 0

# Topics
topics:
  # The main topic is always the base topic that is used.
  _root:
    name: $name # Optional
    accept:
      - food
      - source
    reject:
      - deficiency
    subtopics:
